# Security Implementation Boundary

This directory contains dependency-free, testable security contracts for future agentic capabilities.

The repository is currently pre-production. These files do not claim to provide a production sandbox, credential broker, network egress proxy, or autonomous-agent runtime.

Runtime infrastructure must implement these contracts at the identity, operating-system/container, network, secrets, and application layers.